const appConfig = defineAppConfig({
    ui: {
        colors: {
            neutral: "neutral",
            primary: "orange",
            secondary: "amber"
        }
    }
})

export default appConfig